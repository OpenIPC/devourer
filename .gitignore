compile_commands.json
build
# Out-of-tree build dirs: build-asan (DEVOURER_SANITIZE), per-config trees.
build-*/
.cache
__pycache__/
*.pyc
.venv/
.pytest_cache/

# reference/ vendor drivers are git submodules (see reference/README.md).
# Ignore only the compiled chip-id probe if built locally from tests/.
tests/chipid_probe

# Local lab notes / Claude Code state (never commit)
INVENTORY.md
.claude/

# Bench-local UHD radio selection (tests/uhd_select.py) — serials are
# per-bench, never committed.
tests/.uhd_args
